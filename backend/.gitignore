.env
.venv/
__pycache__/
*.py[cod]

# Local DB artifacts (we use Postgres; don't track SQLite files)
db.sqlite3*
*.sqlite3

# Local/static build artifacts
staticfiles/
media/

# Local dumps/fixtures (keep locally, don't commit)
*.dump
data*.json

# OS/editor noise
.DS_Store
